# LLM Finance Arena - Frontend Environment Variables
# Copy to .env.local and adjust the API base URL if your backend runs elsewhere.
# Base URL for the backend (no trailing /api).
VITE_API_BASE_URL=http://localhost:8080
